def area_of_Rectangle(length, width):
    return length * width
length = float(input('length = '))
width = float(input('Width = '))
area = area_of_Rectangle(length, width)
print("Area of a Rectangle", length, "and", width, " = ", area)